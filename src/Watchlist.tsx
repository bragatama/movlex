import { Box, Container } from "@mantine/core";

const Watchlist = () => {
    return (
        <Box>
            <Container size={"mainXl"}>hello</Container>
        </Box>
    );
};

export default Watchlist;
